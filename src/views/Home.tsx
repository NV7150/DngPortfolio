import React from "react";
import HomeTop from "../Home/HomeTop";

type HomeProps = {

}

const Home = (props: HomeProps) => {
    return (
        <div>
            <HomeTop />
        </div>
    );
};

export default Home;