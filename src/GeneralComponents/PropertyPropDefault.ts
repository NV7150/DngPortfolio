import {PropertyProps} from "./Property";

const defaultProp: Partial<PropertyProps>  = {
    fontSize: "1rem",
    xs: 3,
    sm: false,
    md: false,
    lg: false,
    xl: false
}

export default defaultProp;